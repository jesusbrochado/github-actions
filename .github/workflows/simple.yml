name: Shell Commands

on: [push, pull_request]

jobs: 
  run-shell-command:
   runs-on: ubuntu-latest
   steps:
    - name: actualizar repositorio
      run: git clone https://github.com/jesusbrochado/github-actions.git
    - name: deploy
      run: |
        node -v
        npm -v
    - name: test
      run: ls -lh
